# Changelog

> 注：本文件记录各版本发布当时的状态，测试数量等统计为对应版本的历史数据。

## 0.4.0 - Unreleased

### 🚧 架构先行（T0）

- 新增编辑/导出领域模型骨架（core/domain）
- 新增导出与批处理端口骨架（core/ports）

### 🚧 架构先行（T1）

- 新增编辑用例与批处理用例骨架，并接入应用服务
- 新增统一任务状态模型（运行中/成功/失败/取消）并接入 UI 任务流

### 🚧 架构先行（T2）

- 新增解码后端抽象，拆分标准格式解码实现（infrastructure/image_decoder）
- FsImageSource 接入可替换解码后端，形成标准后端最小闭环

### ✨ 只读增强（v0.4.0）

- 新增幻灯片播放/暂停与间隔配置（1s/2s/3s/5s）
- 新增幻灯片末尾行为配置（循环播放 / 末尾停止）
- 新增前后台切换自动暂停与恢复计时
- 新增 `R / Shift+R` 只读旋转与 `H / V` 只读翻转快捷键
- 新增只读变换菜单分组，并以内存纹理变换实现（不落盘）

## 0.3.5 - 2026-03-18

### ✨ 新增功能

- 新增快捷键 `B`：查看器背景色在黑/灰/白之间循环切换
- 背景色切换结果写入配置并持久化（重启后保持）
- 新增快捷键 `Cmd/Ctrl + 2`：适应图片宽度
- 新增快捷键 `Cmd/Ctrl + 3`：适应图片高度

## 0.3.4 - 2026-03-17

### ✨ 新增功能

- 新增快捷键 `Ctrl + Shift + O`：快速打开文件夹并加载画廊
- 新增快捷键 `Ctrl + C`：复制当前图片到系统剪贴板
- 新增快捷键 `Ctrl + Shift + C`：复制当前图片路径

## 0.3.3 - 2026-03-13

### ✨ 新增功能

- **主题切换功能**：支持四种主题模式
  - 跟随系统：自动适配系统主题设置
  - 浅色模式：明亮的界面风格
  - 深色模式：舒适的暗色界面
  - OLED 纯黑：纯黑背景 (#000000)，专为 OLED 屏幕优化
  - 主题切换即时生效，重启后记住上次设置
  - 通过菜单栏「视图」→「主题」进行切换

### 🔧 技术实现

- 添加 `Theme` 枚举类型（System/Light/Dark/OLED）
- 在 `AppConfig` 中添加 `theme` 字段持久化存储
- 实现主题应用逻辑，使用 egui Visuals 系统
- 添加完整的中英文国际化支持

## 0.3.1 - 2026-03-13

### ✨ 新增功能

- **缩放控制面板**：查看器右下角添加 [−] [百分比] [+] [1:1] 控制按钮
  - 1:1 按钮：快速重置到原始尺寸 (100%)
  - −/+ 按钮：精确缩放控制
- **信息面板增强**：左下角显示文件名、尺寸、MP、文件大小
  - 文件名超长时自动截断，鼠标悬停显示完整名称

### 🔧 修复与优化

- **错误处理优化**：逐步将 `let _ =` 改为显式错误处理，统一补充日志
- **代码结构优化**：拆分 700+ 行的 `update` 函数，降低复杂度
- **代码质量**：修复所有 cargo clippy 警告
- 修复配置保存防抖处理问题

### 📝 文档

- 全面审查并修复文档与代码不一致问题
- 统一测试数量描述为 250+（以文档口径为准）
- 统一架构名称为"分层架构"
- 更新目录结构描述与实际代码一致

## 0.3.0 - 2026-03-03

### 🏗️ 架构重构 - Clean Architecture

本次版本进行了重大架构重构，采用 Clean Architecture 设计理念，将代码组织为清晰的分层结构，提高可维护性、可测试性和可扩展性。

#### 📁 新目录结构

```
src/
├── main.rs              # 应用程序入口点
├── config.rs            # 配置管理（Domain层）
├── app/                 # 核心应用逻辑（Domain层）
│   └── mod.rs           # OASImageViewerApp 主控制器
├── viewer/              # 图像查看器（Application层）
│   └── mod.rs           # Viewer 组件
├── gallery/             # 图库模块（Application层）
│   └── mod.rs           # Gallery 组件
├── decoder/             # 图像解码器（Application层）
│   └── mod.rs           # ImageDecoder
├── dnd/                 # 拖放处理（Infrastructure层）
│   └── mod.rs           # 拖放事件处理
├── clipboard/           # 剪贴板操作（Infrastructure层）
│   └── mod.rs           # ClipboardManager
├── info_panel.rs        # 信息面板（UI层）
├── shortcuts_help.rs    # 快捷键帮助（UI层）
└── utils/               # 工具函数（Infrastructure层）
    ├── mod.rs           # 通用工具函数
    ├── errors.rs        # 错误类型定义
    └── threading.rs     # 线程工具
```

#### 🏛️ 架构分层说明

| 层级 | 模块 | 职责 |
|------|------|------|
| **Domain** | `app`, `config` | 核心业务逻辑、应用状态管理 |
| **Application** | `viewer`, `gallery`, `decoder` | 功能组件、图像处理、解码 |
| **Infrastructure** | `utils`, `dnd`, `clipboard` | 工具函数、系统交互、I/O操作 |
| **UI** | `main.rs`, `info_panel`, `shortcuts_help` | 界面渲染、用户交互 |

#### ✅ 测试覆盖

- **265+ 单元测试** 通过
- 测试分布：
  - `app`: 90+ 测试用例（状态管理、导航、快捷键）
  - `viewer`: 60+ 测试用例（缩放、渲染、图像操作）
  - `gallery`: 70+ 测试用例（缩略图、导航、选择）
  - `decoder`: 50+ 测试用例（格式检测、解码）
  - `dnd`: 40+ 测试用例（拖放处理、文件过滤）
  - `clipboard`: 30+ 测试用例（复制操作、错误处理）
  - `utils`: 40+ 测试用例（工具函数、错误类型）
- 集成测试：`tests/` 目录包含 decoder 和 dnd 集成测试

#### 🔄 迁移说明

**从 v0.2.x 迁移到 v0.3.0：**

1. **配置文件**：配置文件位置保持不变，自动兼容
2. **API 变更**：内部模块结构重组，对外功能不变
3. **构建命令**：无变化，`cargo build --release` 即可
4. **依赖更新**：无新增依赖，现有依赖版本保持兼容

#### 🐛 修复

- 修复了缩略图异步加载的内存泄漏问题
- 优化了图像缩放算法，提高性能
- 修复了全屏模式下菜单栏显示问题

#### ⚡ 性能优化

- 缩略图采用后台线程异步加载
- 图像解码增加双重容错机制
- 配置保存增加防抖处理

---

## 0.2.0 - 2026-02-15

### 新增功能

- 🖼️ **画廊模式**：G键切换，支持缩略图网格浏览
- 📁 **拖放支持**：支持拖拽图片文件到窗口打开
- ⌨️ **快捷键系统**：完整快捷键支持，? 键显示帮助
- ℹ️ **信息面板**：F键切换，显示图片元数据
- 📋 **剪贴板集成**：右键菜单复制图片和路径

### 改进

- 优化了图像加载性能
- 改进了中文界面显示
- 增加了更多配置选项

---

## 0.1.0 - 2026-01-20

### 初始版本

- 🚀 首次发布
- 📷 支持 PNG/JPEG/GIF/WebP/TIFF/BMP 格式
- 🔍 基础缩放和平移功能
- 🖥️ 全屏模式支持
- ⚙️ 基础配置系统
