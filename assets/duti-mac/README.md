# MacOS 默认程序设置

## 说明
MacOS 支持原生方式设置默认程序，无需额外工具。

## 使用方法

### 方式 1：使用应用内菜单
帮助 → 系统集成 → 设为默认图片查看器
（如果失败，请使用方式 2）

### 方式 2：手动设置（推荐）
1. 右键任意图片文件
2. 选择「显示简介」
3. 在「打开方式」中选择「OAS Image Viewer」
4. 点击「全部更改」

## 技术说明
应用通过 LaunchServices API 尝试自动设置默认程序。
如果系统限制导致失败，会提示用户手动设置。
